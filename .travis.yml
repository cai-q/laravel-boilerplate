language: php

php:
  - '5.6'
  - '7.0'
  - 'hhvm'
  - 'nightly'

before_script:
  - composer install --dev --prefer-source --no-interaction
  - mysql -e 'create database laravel-boilerplate;'
  - cp .env.travis .env

script: phpunit

services:
  - mysql

